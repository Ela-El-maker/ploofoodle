# Ploofoodle — Architecture

Overview

- Ploofoodle is a PHP front-end/web entry that integrates with Plonkadoodle (backend services) and uses MySQL for persistence.

Key components

- `index.php` — entrypoint and routing
- `src/` — application logic
- `public/` — public assets
